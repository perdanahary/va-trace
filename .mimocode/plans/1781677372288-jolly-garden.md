# Plan: Simplify Production Statuses to 3-Stage Pipeline

## Goal
Reduce production statuses from 6 (Accepted → Printing → Finishing → QC → Ready → Completed) to 3 stages: **Accepted → In Progress → Done**.

## New ProductionStatus Type
```
NEW | SUBMITTED | ACCEPTED | IN_PROGRESS | COMPLETED | CANCELLED | EXCEPTION
```

`IN_PROGRESS` replaces: `PRINTING`, `FINISHING`, `QUALITY_CONTROL`, `READY_FOR_DISTRIBUTION`

## Files to Modify

### 1. Type Definitions
- `src/lib/types/v2/status.ts` — Remove PRINTING/FINISHING/QUALITY_CONTROL/READY_FOR_DISTRIBUTION, add IN_PROGRESS
- `src/lib/types/status.ts` — Same changes for legacy type

### 2. Store & Logic
- `src/lib/v2/productionStore.ts` — Update ALLOWED_TRANSITIONS, remove status-specific timestamp logic (startedAt, qcStartedAt), simplify ready quantity validation
- `src/lib/orderStatus.ts` — Update status ranking and production status checks
- `src/lib/v2/selectors/derivedStatus.ts` — Update production status checks
- `src/lib/v2/compat/legacyLabels.ts` — Update label mappings

### 3. UI Components
- `src/components/domain/ProductionPipelineStepper.tsx` — Simplify to 3 steps: Submitted → Accepted → Done
- `src/components/domain/badges/badges.tsx` — Map IN_PROGRESS to "processing"
- `src/components/ui/StatusBadge.tsx` — Map IN_PROGRESS to "processing"

### 4. Page Components
- `src/pages/vendor/VendorOrderDetail.tsx` — Update UPDATABLE_STATUSES, simplify dialog logic
- `src/pages/admin/ProductionQueue.tsx` — Update UPDATABLE_STATUSES, simplify dialog logic
- `src/pages/vendor/VendorDashboard.tsx` — Update filter logic
- `src/pages/vendor/VendorOrders.tsx` — Update filter logic
- `src/pages/client/ClientDashboard.tsx` — Update filter logic

### 5. Seed Data
- `src/lib/v2/seed/seedBuilders.ts` — Update seed data to use new statuses

## Transition Rules (New)
```
NEW → SUBMITTED, CANCELLED
SUBMITTED → ACCEPTED, CANCELLED, EXCEPTION
ACCEPTED → IN_PROGRESS, CANCELLED, EXCEPTION
IN_PROGRESS → COMPLETED, CANCELLED, EXCEPTION
COMPLETED → (terminal)
CANCELLED → (terminal)
EXCEPTION → IN_PROGRESS, CANCELLED
```

## Verification
1. Run `npm run lint` to check for type errors
2. Run `npm run build` to verify compilation
3. Test vendor order detail page — "Update Production" button should show 3 options
4. Test admin production queue — same 3 options
5. Verify stepper shows: Submitted → Accepted → Done
